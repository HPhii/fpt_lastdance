<mvc:View controllerName="z.wf.zwfmanagement.controller.MainView"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m">
    <Page id="page" title="{i18n>title}" showNavButton="true" 
      navButtonPress=".onNavBackToDashboard">
        <content>
            <Table id="idProductsTable"
                inset="false"
                items="{
                    path: '/WfTasks',
                    sorter: { path: 'CreationDate', descending: true }
                }">
                <headerToolbar>
                    <OverflowToolbar>
                        <Title text="{i18n>mainViewTaskList}" level="H2"/>
                    </OverflowToolbar>
                </headerToolbar>
                <columns>
                    <Column><Text text="{i18n>mainViewTask}" /></Column>
                    <Column><Text text="{i18n>mainViewDescription}" /></Column>
                    <Column hAlign="End"><Text text="{i18n>mainViewCreationDate}" /></Column>
                    <Column hAlign="End"><Text text="{i18n>mainViewPriority}" /></Column>
                </columns>
                <items>
                    <ColumnListItem type="Navigation" press=".onListItemPress">
                        <cells>
                            <ObjectIdentifier title="{TaskText}" text="{WorkItemID}"/>
                            <Text text="{WorkItemText}" />
                            <Text text="{CreationDate}" />
                            <ObjectStatus text="{Priority}" state="{= ${Priority} === '1' ? 'Error' : 'None' }" />
                        </cells>
                    </ColumnListItem>
                </items>
            </Table>
        </content>
    </Page>
</mvc:View>